money:
  name: '金币'
  action:
    # 余额查询
    hook: economyBalance(sender)
    # 余额提取
    withdraw: economyWithdraw(sender, arg)
    # 余额存入
    deposit: economyDeposit(sender, arg)
    # 余额设置
    set: economySetBalance(sender, arg)
