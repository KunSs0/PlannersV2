money:
  name: '金币'
  action:
    # 余额查询
    hook: sender :: getBalance()
    # 余额提取
    withdraw: sender :: takeBalance(&arg)
    # 余额存入
    deposit: sender :: giveBalance(&arg)
    # 余额设置
    set: sender :: setBalance(&arg)
